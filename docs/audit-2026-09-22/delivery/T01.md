# T01 交付说明 · 干净启动与质量基线（P0）

工作包 / 对应发现：T01；对应 F07、F29、F33、F35、F36（F42 启动文档部分）。

当前基线 / 实际修改路径：
- 基线 `47e55c0`（与审计基线一致），执行时 HEAD 未变。
- `apps/server/package.json`：声明 `"sharp": "^0.35.4"`（原为 extraneous，静态 import 于 `modules/media/compress.ts:8`、`label.ts:14`）。
- `apps/server/scripts/extract-meta.mjs`：显式 `import { URL } from 'node:url'`（no-undef）。
- `apps/server/scripts/gen-corpus.mjs`：6 个字面空格改 `{6}` 量词（no-regex-spaces）。
- `apps/web/scripts/spot-check.mjs`、`apps/web/scripts/ui-audit.mjs`、`scripts/audit-shots.mjs`：7 处 `catch {}` 补意图注释（no-empty）。
- `scripts/audit-touch.mjs`：校准为解析 `apps/web/src/v8.css` 的 min-height/height 级联（后写覆盖先写、跳过注释与后代选择器），阈值改为 V8 Phase 8 红线（孩子端下限 44 / 家长端 40）。
- `e2e/playwright.config.ts`：继承环境剥离全部真实供应商变量（TTS_BASE/TTS_API_KEY/TAO_IMAGE_*/TAO_VIDEO_*）+ `DOTENV_CONFIG_PATH` 指向不存在文件（防止 demo 内 `dotenv/config` 回读 `apps/server/.env` 的 7 行真实 key）；端口支持 `TAO_E2E_API_PORT/TAO_E2E_WEB_PORT` 覆盖（用户实例占用 5173/8787 时不劫持）。
- `apps/server/src/demo/main.ts`：启动横幅明示已接线的真实 AI 供应商（澄清「mock 网关零出网」仅指微信读书）。
- `apps/web/vite.config.ts`：`/api` 代理目标支持 `TAO_API_PORT` 覆盖。
- `.github/workflows/ci.yml`（新增）：install→prisma generate→typecheck→lint→unit→web build→e2e（隔离自起服务）。

复现前结果（与审计记录一致）：
- `npm ls sharp --all` → `sharp@0.35.4 extraneous`；manifest 无声明。
- `npm run lint` → 8 errors（URL no-undef ×1、no-regex-spaces ×1、no-empty ×6）。
- 后端单测 293/294（`content.test.ts > 重复入库不产生重复章节` 60s 超时）。
- `npm run audit:touch` → 40 文件 68 flags，全部为「未声明」（检查器不认识 v8.css）。
- `npm audit --omit=dev`（官方源）→ 4 条（2 high / 2 moderate）。

最终行为与实现边界：
- lint 0；touch 检查器只报真实小目标（68→10：换人浮钮 ~16px、6 处 linklike 文字链、2 处 42px 导航/朗读钮、1 处 main-action），全部移交 T07 处置；`npm run verify` 因此仍失败——这是真实缺陷非工具误报。
- seed 超时根因=用例双次全量播种（222 本×2，单跑 42.6s、套件资源争用时撞 60s）。幂等断言改用最小真实包复验同一 delete-then-create 路径，全量播种保留一次供台账断言；单测总时长 210s→124s。
- 依赖公告 4 条均无兼容范围内补丁（修复全部落在 Fastify 5.12.5 / Router 7.18.4 大版本），按任务书不做 `audit fix --force`，逐条补偿措施：
  - GHSA-jx2c（Content-Type 绕过）+ GHSA-w2qp（原始类型强制绕过）：业务校验是路由层手写 zod（`parse()`），不依赖 Fastify schema 校验，绕过面有限；
  - GHSA-444r（X-Forwarded 伪造）：`trustProxy` 默认 false（本次已落实配置项），伪造头不影响 request.ip；
  - GHSA-mrq3（sendWebStream DoS，low）：未使用 web stream 发送路径；
  - find-my-way GHSA-c96f（HTTP/2 DDoS）：服务仅监听 HTTP/1.1，未启用 h2。
- 全新目录验证：拷贝工作树（未授权提交，clone 会验证旧基线）→ 删 `.env` → `npm ci` → prisma generate/migrate → 生产入口 `src/index.ts` 启动（端口 8899，health 200）→ web 生产构建，全部通过。

迁移与旧数据处理：无业务迁移（lockfile 变更 + sharp 声明）。

运行的命令、通过/失败数量：
- `npm run lint` 0 错误；`npm run audit:touch` 10 项真实残留（原 68 误报）。
- `npm audit --omit=dev --registry=https://registry.npmjs.org`：4 条未修，理由如上（证据 `evidence/npm-audit-production-after-T01.json`）。
- server vitest 316/316（含 seed 超时修复与 T02/T03/T04/T06 新增用例）；web vitest 125/125；typecheck 双端 0；eslint 0。
- 全量 e2e（隔离端口 18887/15173）：**17 过 + 1 跳 + 0 失败**。
- 全新目录四步验证通过（脚本日志留存于执行记录）。

浏览器场景、视口、截图路径：本包无 UI 行为变化；浏览器验收由 T02/T03 包承担。

未验证项及原因：
- CI「真正触发」需推送后观察 GitHub Actions——任务书未授权提交/推送，标为未验证。
- 公告的框架级升级（Fastify 5 / Router 7）属独立变更，需框架兼容验收，本包未做。
- `npm run verify` 仍被 10 项真实触达残留阻断（T07 范围）。

回滚步骤与残余风险：恢复 lockfile/manifest 与各脚本文件即可，无数据迁移。残余风险：依赖公告在框架升级前持续存在（补偿措施见上）。

是否提交/部署：未授权，未提交、未推送、未部署。
