# T04/T06 交付说明（第一批）· 心情枚举 / 严格 boolean / TTS 语言

工作包 / 对应发现：T04 第一批（F11、F12 的 DATA-01 子项）；T06 第一批（F16）。
按任务书「一个工作包解决一个一致问题」，完整的游标/阅读轮次/完成事件三模型拆分（T04 主体）、
音频会话状态机（T06 主体）不在本批，见「未验证项与移交」。

当前基线 / 实际修改路径：T01–T03 之后的工作树。

- **F11** `packages/shared/src/index.ts`：`MOODS` 增加 `curious`（好奇）并以注释声明唯一事实源；
  `modules/cosession/routes.ts` 与 `service.ts` 的 zod/白名单一律从 `@taoread/shared` 派生，
  两端手抄枚举从此不能再漂移。客户端结算浮层四档（开心/惊喜/安静/好奇/想一想）全部可提交。
- **F12/DATA-01** `content/routes.ts`：`completed: z.coerce.boolean()` → `z.boolean()`，
  probe 场景「completed:'false' 字符串返回 finished=true」变为 400 VALIDATION。
- **F16** `modules/tts/routes.ts`：整章合成语言从书目推导（`book.lang === 'en' ? 'en' : 'zh'`），
  删除 `'zh' : 'zh'` 死代码；`cacheKey` 增加语言维度（中英同文本缓存隔离），
  preview 与整章两条路径都带上语言。

新增测试 `apps/server/test/facts.test.ts`（3 例全过）：
1. DATA-01：completed:'false' → 400，且不留任何 finished 脏数据；
2. F11：shared 全部 6 种心情参数化收尾 200，非法心情 400；
3. F16：英文书整章合成，注入假上游捕获请求体，instruction 必含英文朗读指令。

未验证项与移交（T04/T06 主体，工作量与迁移见任务书，未在本批展开）：
- T04 主体：ReadingCursor / ReadingRun / ReadingCompletion 三模型拆分、
  「完成后重开前章清除 finished」（DATA-02）、「末章 100% 未完成冒充」（DATA-03）、
  报告事件快照口径与家庭时区（F14）、进度保存队列与多设备 revision（F13）——
  需要 Prisma 迁移 + 历史数据「完成时间未知」迁移策略 + 双写过渡，按任务书应由单一负责人顺序集成。
- T06 主体：AudioSession 状态机（waiting/playing/done/cancelled 贯通取消链、
  第二段延迟续播、切书防串流）、voice 能力校验、真实听审（agent 不可替代）。

是否提交/部署：未授权，未提交、未推送、未部署。
