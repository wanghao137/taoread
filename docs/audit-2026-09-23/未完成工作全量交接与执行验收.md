# 桃阅读未完成工作全量交接与执行验收（2026-09-23）

> 交给下一位执行 agent 的工作底稿。**状态是 2026-09-23 本地快照，不是上线证明，也不是逐行穷尽证明。**接手时先核对最新 HEAD、`git status`、实际服务与 Cloudflare 控制台，再以代码、迁移、真实样本和运行证据关闭任务。本文不包含密钥和用户数据。

## 0. 范围、基线与证据等级

- 仓库 `D:\codesolo\taoread`，远端 `wanghao137/taoread`，本次读取的 HEAD `e721d1b656d06484e1b0658dcd3bde98e05670a8`。**重要：当前实现有大量已修改及未跟踪源码、迁移、测试和一个 Word 报告；HEAD 并不代表当前产品代码，远端也不代表此工作区。**接手者须记录 diff 清单，保留这些改动，禁止重置、覆盖或把 `dev.db` 当生产库。先确认谁负责集成，涉及同一 schema、`V8App.tsx`、`ReaderPage.tsx` 时顺序协作。
- 基础材料：[原审计](../audit-2026-09-22/01-全面审计报告.md)、[任务书](../audit-2026-09-22/03-Agent执行任务书.md)、[拼读方案](../audit-2026-09-22/02-英语自然拼读方案.md)、[旧复核](执行完成度复核.md)、[部署清单](Windows常开电脑-CloudflareTunnel上线清单.md)。旧复核和[早期草稿记录](家庭导入与自然拼读草稿实施记录.md)是**变更前快照**；其中媒体“任意 `fam:` 可读”、进度读取无屏蔽校验、回看前章清除完成、触达检查 10 项失败、EPUB/PDF 完全不支持、拼读仅一课等说法，不能再当当前事实。
- 本地上轮证据：服务端 332/332、前端 125/125 单测，lint、typecheck、前端 build 通过，`npm run audit:touch` 0 违规；并非本次撰文重新运行。构建有原有动态/静态导入提示。**未完成**真实 PDF 正向样本、Gutenberg 全链路、移动端/键盘/读屏、完整 E2E、远端 CI、生产迁移与恢复、公网验收；凡随后有源码变更均复跑相应门禁。静态触达 0 违规不能证明计算后样式和真实命中盒。
- 状态口径：**部分**＝有代码/测试但验收或关键子能力缺失；**待验证**＝实现存在但尚无目标环境证明；**未做**＝当前未见可交付实现；**外部阻断**＝需要账户权限、真实合法素材或人工专业签认。不要把“设计文档”“mock 通过”“可访问静态页”写成正式上线。
- 优先级：**P0 公网开放前**：身份恢复及隔离、内容/媒体权限、备份恢复、内容权利/上线范围、关键阅读保存可靠性、生产安全与域名实测。**P1 可用产品**：导入、拼读完整试点、音频、主要 UX。**P2 后续优化**：离线、扩容、代码拆分等；若对外承诺 P2 能力，则升为发布门槛。可选择缩小首发范围，但必须隐藏未验收入口并修改对外承诺，不能把阻断项标为“接受”。

## 1. 执行顺序与共同完成标准

1. **冻结和隔离（G0）**：记录文件/迁移清单与已运行端口，使用独立数据库、媒体目录、空闲端口、mock 上游；核对旧家庭数据、基线缺陷和测试。提交风险、依赖、安全与版权清单。不要触动其他服务、主站 DNS 或真实供应商付费接口。
2. **可信核心（G1）**：完成 WP-A 身份与授权、WP-B 阅读事实与保存；用新旧家庭、跨家庭、禁用书、会话撤销、双设备乱序、网络失败逐一验收。
3. **内容能力（G2）**：WP-C 合法导入和逐格式真实样本；WP-D 内容清权/原子发布；WP-E 自然拼读先审教材音频，再实现整条路径；WP-F 音频与 UI。各包可分工，数据库迁移和共享组件只允许一个集成负责人。
4. **运维与发布（G3）**：WP-G 备份/恢复、性能/隐私/CI、可回滚部署；先私有测试 hostname，再切 `read.taostudioai.com`，公网 HTTPS/手机网络/深链/权限/续读逐项实测，留下可回退证据。正式课程与全量书库上线须人工教学/权利签认。
5. 每包交付一页证据：复现条件、代码/迁移路径、前后 API 与 UI、自动/浏览器/真机结果、数据迁移及回滚、尚未验证项、最终发布决定。产品 bug 要有失败状态与恢复路径；不能仅用 happy-path 单测验收。

## 2. 工作包 WP-A：身份、授权、媒体和费用边界（P0）

**当前事实**：`modules/family/service.ts` 已要求独立家长码；`lib/auth.ts` 使用 `sid`，旧无 `sid` 令牌拒绝；`content/routes.ts` 的详情、章节、进度读写和收藏写入已调用 `assertContentReadable`；`modules/media/access.ts` 把家庭 scene 绑定家庭 ID、签 5 分钟 path-scoped 票据；`artRoutes.ts`、`videoRoutes.ts`、`media/routes.ts` 做归属/会话验证；`art/book/:bookId` 已检查屏蔽内容。早期 F02/F03/F04/F05 的完整原始漏洞描述已过时。上述代码并不能证明旧缓存或所有衍生资源安全。

- **A1 家庭迁移/找回**：用旧库副本重现 `parentCode=null` 的既有家庭如何在旧令牌失效后安全恢复家长身份；设计不靠家庭码直接升级、不中断既有儿童、无可枚举的错误码/响应时序。记录迁移 dry-run、撤销、丢失设备、多个孩子与失败回滚。设备列表/单设备退出/撤销 UI、多实例或重启后失效时限仍需实现/量化。
- **A2 单一内容策略**：统一检查列表、详情、章节、进度、收藏、TTS、共读、封面/生成、搜索及本地缓存；屏蔽后旧页面/缓存和已发 5 分钟票据的行为要有明确策略。对两个家庭、家长与孩子、无效 childId、他家书 ID、旧 sid 分别测试直接 API、URL、Range 请求。检查公开素材与私人素材的历史记录归属，迁移或隔离不能追溯确定 owner 的文件。
- **A3 AI/成本**：目前家长生成、家庭 scene 隔离不等于可公开发布。需要平台管理员审核/发布/撤回模型；同 key 并发生成用锁/幂等控制且跨进程有效，限制每家/每日请求、文件大小、磁盘和上游支出；请求取消、超时、失败退款/不计费、过期清理。任意文本 TTS 私有缓存归属已有 `ttsMediaOwner`，仍须测试历史全局缓存、语言/voice/provider/model/文本版本入键、删除生命周期与票据泄漏。默认不配付费密钥时功能降级文案要实测。
- **A4 代理/文件/依赖**：核验 `TAO_TRUST_PROXY` 可信跳数/IP、Cloudflare/Caddy 请求 IP、防伪造 `X-Forwarded-For`、共享出口限流；媒体 realpath 与 Range 已有，补条件请求、头/尺寸、视频拖动和票据日志/Referer/缓存测试。重新运行官方依赖审计，逐条评估当期公告和可达性、更新与回归；旧 2 high/2 moderate 仅是当时结果，不能写为当天结论。
- **完成定义**：旧家庭可恢复、任意家庭码不能获家长身份，撤销与跨家庭/屏蔽后的数据和媒体均按约定拒绝，模拟 20 个并发生成只触发约定数量的上游调用，限制/成本与日志可观察，生产代理链路实测。F01–F08、T02/T03 的剩余见矩阵。

## 3. 工作包 WP-B：可信阅读、进度、续读与家庭书（P0/P1）

**当前事实**：`content/service.ts:reportProgress` 仅在末章显式 `completed` 时设 `finished`，回看前章不会再清掉完成；`content/routes.ts` 的进度 GET、收藏已补屏蔽校验。家庭导入有独立 `ImportedReadingProgress`（每孩子/书的章序、文本 offset、completed），孩子能从目录续读；这比旧复核进展更远，但尚缺并发契约和可靠交互。

- **B1 阅读事实**：区分游标、阅读轮次、不可变完成事件/历史、收藏和报告来源；完成幂等且与末章真实完成一致，回读/重新开启不抹历史。对老数据迁移/异常章节数/报告、日历、夜灯及家庭时区，制定转换和回滚；数据写入事务与故障注入。`finished` boolean 保留还是扩展，以需求与报告口径决定，不为“好看模型”无谓迁移。
- **B2 保存与双设备**：主阅读器 `ReaderPage.tsx` 的节流/失败提示/离开提交/后台暂停/网络恢复，增加保存队列、重试及可见状态；定义 revision/冲突处理，双设备乱序不能让旧进度静默覆盖新进度。首页、详情、深链按 ID 独立加载且共同使用最近有效的 `ResumeTarget`，包含 chapter/block；清晰区分 403/404/网络错；搜索和过滤统一查询状态、取消过期响应。跨入睡眠截止时间应实时收尾，会话时长不要宣称专注时长。
- **B3 家庭导入阅读器**：`FamilyTrialPages.tsx` 当前仅选文字再点“保存所选位置”或“读完本章”；下一章/退出/后台默认不自动保存，列表 `Promise.all` 无 catch，目录/进度/章节异步返回存在状态竞态。补自然段/可见位置与保存状态、离开前可靠提交、断线重试、跨设备 revision、加载取消/错误/空态、目录与进度数据顺序；不要把 DOM 文本 offset 在排版/版本变化后当永久稳定锚点。`importRoutes.ts` 的 `PUT progress` 目前直接 upsert `{order, offset, completed}`，旧请求可覆盖且可把 completed 复原 false、非末章可传 true；定义“读完章”与“读完书”，后端只接受合法完成位置、恢复时有明确完成态。删除书应清进度并向家庭说明不可恢复；直接 API 测角色、年龄段与他家档案。
- **完成定义**：首次/续读/跳章/末章/回看、刷新/关闭/断网/双设备、空家庭、删书、屏蔽书及旧版本正文变更均有自动与真实浏览器证据；保存失败不会假称已保存。F09–F15、F20、T04/T05 对照矩阵。

## 4. 工作包 WP-C：TXT/EPUB/PDF 与合法来源导入（P0/P1）

**已有**：`content/importRoutes.ts` 家长上传 TXT/EPUB/PDF，文件名/体积校验、sha256 家庭内去重、私有正文/章节、家庭和年龄隔离、删除、Gutenberg 固定白名单；`extractBook.ts` 对 EPUB 压缩大小/比率/路径检查、PDF 文本提取（≤4 MB、≤100 页），`schema.prisma` 有独立导入与进度表。`test/extractBook.test.ts` 为 EPUB 正向、PDF 伪造负向；`test/importBinary.test.ts` 为 EPUB API 正向；`test/publicDomain.test.ts` 仅白名单/权限/未知 ID 不出网。**未以真实文本 PDF 和 Gutenberg 远端抓取→入库→续读证明端到端成功。** TXT 文本最多 4 MB/160 章/每章 100k 字；EPUB 8 MB 且解压文本上限 4 MB。复杂排版、插图、脚注、超链接、公式、扫描件 OCR、DRM 不支持，须明确 UI 产品边界。

- **C1 格式与安全**：用经授权的真实中英文 TXT、标准/复杂 EPUB、文本 PDF、扫描/加密/损坏 PDF、压缩炸弹、路径编码、超长正文等夹具做解析与 UI 验收。当前 EPUB 用正则找 XML/XHTML 的 manifest/spine 并剥标签，容易丢实体/排版/读序/脚注；检查解码 URI、内外链、样式脚本、ZIP 实际解压上限/内存、PDF 解析器版本及 Node 24 兼容。明确只支持“提取纯文本”还是要真正的 EPUB/PDF 版式阅读；若宣称导入电子书可读图文，则需资源沙箱、格式渲染及资源预算。正文丢失内容应告知家长并提供预览/取消。
- **C2 数据契约**：预览标题/作者/语言/封面/章节/正文统计，检测乱码与章节解析异常；重命名同哈希、重新导入同文件的年龄/元数据更新策略；失败和重复请求原子性；保留 `sourceName` 的隐私最小化。电子书删除及家庭删除覆盖章节、进度、备份保留期；导出能力与隐私说明。家长“确认使用权”只是声明，不能替代授权。
- **C3 网站抓取**：当前仅 `gutenberg-11`《Alice's Adventures in Wonderland》固定 ID、固定 URL、家长角色、大小/10s/禁重定向。用可控源模拟 200/超时/跳转/截断/超限/编码/重复；真实来源经人核对当地版权、具体文本版本、免责声明/版权页处理、儿童适龄，再完成端到端导入与续读。`jbiaojerry/ebook-treasure-chest` 是资源聚合入口，不是单本书的转载授权；`z-lib.id` 的站点身份、合法授权/API 与获取方式未经确认，**未接入，也不能为了完成需求绕过付费/版权/访问限制或进行任意 URL 抓取**。需逐书来源 URL、作者/译者、出版/改写版本、授权地域与许可、授权存证、可自动获取方式、撤回与复查机制；无法证明时保留合法自有文件上传，或选明确授权/公版来源。以后扩充白名单先做服务端域名/IP/重定向/SSRF 与流量限制。
- **完成定义**：每种宣称支持的格式均有真实正向样本和拒绝样本，经浏览器上传、目录、正文、续读、断线/删除；每个自动来源都能提供权利证据和固定安全抓取路径；生产选择性开放，不应把“聚合网站可搜索”写成“书籍可合法下载”。

## 5. 工作包 WP-D：自然拼读完整流程与可解码书（P1，公开课程前阻断）

**已有**：`modules/phonics/lessons.ts` 两节**无音频字形题草稿**，两篇仅由 s/a/t/p 拼写构成的**原创短文草稿**；`routes.ts` 家长按孩子开启、目录、attempt/response/finish/summary 幂等处理；`FamilyTrialPages.tsx` 孩子端实际只取 `catalog.lessons[0]`，可暂停但没有恢复；默认关闭、文案明确是内测。课程数据模型已迁移；这既非“完全未做”，也非可发布课程。

- **D1 教材策划**：从[拼读方案](../audit-2026-09-22/02-英语自然拼读方案.md)拟定 8 课（包括复习），锁口音、教学目标、GPC 顺序、词和音素分割、例外词/大小写、口语词义和适龄。用机器校验所有词/句/书只用已教 GPC 或显式教过的例外；未教 `c` 的 `cat`、弱读 `a` 等要拦截。至少一整课从家长开启→观察准备度→听示范→看字听音→合成→分音→独立解码短读物→温和结束→错题复习→家长报告闭环后再铺 8 课。音频不能用通用字母名称 TTS 冒充纯音素。
- **D2 音频与读物**：制作/取得授权的音素、慢速合成、正常词音、句子示范，记录口音、发音人/来源、授权、版本、hash、文本-音频对照；逐条专业听审，音频缺失或未批准禁止发布。两篇草稿逐词教学审校并扩充对应 8 课的可解码原创书/短篇（标题、插图、级别、所需 GPC、例外、教师签名、内容版本）；不可直接标“自然拼读书”。自有原创也要素材资产授权和审校。
- **D3 数据/交互**：版本化课程/撤回与迁移、共享 DTO/API；题目不泄漏答案，提示与独立作答分别记录；按孩子隔离、家长可关闭且隐藏入口；暂停/关闭/刷新/离线后可继续，重复 response/finish 幂等；错题复习要解释性推荐而非单次正确率判“掌握”。家长可见真实练习记录、未开始/暂停/完成；无录音/ASR 时不要宣传发音诊断。阅读器与课程文本使用年龄适当的可访问 UI。
- **完成定义**：8 课/相应短读物教材与权利签审、全量音频听审、课程校验器、四类练习和纵向路径、家庭/孩子隔离及中断恢复，真实手机试点完成；未取得教学签审时维持默认关闭与“草稿”标签，不称学习效果已证实。

## 6. 工作包 WP-E：UI/UX/UE、音频及家长路径（P1）

- **E1 阅读与导航**：`V8App.tsx` 详情重复 Shell、手机 ≤460px 的详情次级动作隐藏、阅读器双滚动与底栏横溢、首页主操作/儿童目标尺寸、空态及 200% 缩放均需按 320/360/390/768/桌面真浏览器复核并修复；常见阅读任务从“今天/找故事/我的”进入、返回和错误恢复保持一致。触达静态 0 违规仅是起点。更新设计 tokens/KitchenSink 和检查器，测计算后的字体/触点、对比度、长文、简体中文文案和 reduced motion。
- **E2 无障碍**：`ReaderPage.tsx` 弹层补 dialog/aria-modal、焦点限制/归还、Esc、背景 inert；键盘/读屏读序/动态状态、按钮名称、触屏与 200% 缩放；不得靠改 CSS 数值通过静态脚本代替真实验收。
- **E3 音频**：英文整章 `lang` 已接入，仍需 `audioPlayer.ts` 的等待/stream done/Abort/取消完整状态机，切书/暂停/超时/慢网/重复播放不串流；TTS voice、provider/model/文本版本缓存兼容及真人听审，统一 note 朗读块。生成 AI 禁用时保留可理解的浏览器朗读降级与错误提示。
- **E4 外部/家长体验**：`ContentHub.tsx` 纸书占位须完成录入→共读→历史/报告或明确移除入口；`BindWizard.tsx` 微信读书解绑/重试/撤销/缓存清理、来源与授权账号真实路径；词卡需上下文/发音/删除和 `(childId, word, lang)` 去重迁移；报告/记忆刷新、来源标签、夜灯/日历日期口径和中文状态一致。年龄阶段不等于自主阅读或英语解码等级。先盘点外部接口授权与数据处理政策，不擅自操作真实第三方账号。
- **完成定义**：每个对外可见入口能完成真实家庭任务；4 类视口和键盘/读屏通过，音频断续/切换不串流，失败可恢复。现有家长周报与纸书、微信读书、家庭导入/拼读的指标不能混用口径。

## 7. 工作包 WP-F：内容权利、数据质量和发布控制（P0）

- **F1 逐书审签**：现有约 222 本目录的每**具体文本版本**追溯作者、译者、出版/改写、来源 URL/纸本、许可地域和期限、CC 署名链、是否全本/节选/改写、儿童适龄、拼音/注释/断句/英语难度；维护 rights manifest 与审校状态及对应页面版权说明。没有证据的书先下架/不发布，版权与语文编辑分别签认；“公版原作”不自动覆盖现代译文/改写。
- **F2 原子内容发布**：`content/seed.ts` 重导入会先删章节，需书级事务或版本指针、稳定 chapter/block 键、进度迁移和故障注入；断电、重复导入、旧版回滚不能丢可读正文/游标；内容媒体版本一致。`init:content` 已有，但空库与存量库的完整初始化/回退要独立复测。
- **F3 产品边界**：README、关于页、来源、许可、家长说明、注释中“不分发正文/阅读在其他平台”等历史承诺与当前书库矛盾；待审书不能通过前端隐藏代替服务端权限。家长上传内容和公版导入需区分私有与公开，不能互相传播。
- **完成定义**：每本上线书有可追溯、人工批准的 manifest；导入中断/回滚仍可读旧版、旧进度有定义；产品描述只承诺真实能力。

## 8. 工作包 WP-G：零新增预算、备份与公网发布（P0）

**当前机器快照**（2026-09-23，需接手者复核）：Windows 常开机，Node 24、`cloudflared` 服务 Running/Automatic；约 1.81 GB/3321 个媒体文件、本地 `dev.db` 约 6.9 MB。`C:\ProgramData\cloudflared\config-svc.yml` 当前只有 `cpa.taostudioai.com → 127.0.0.1:8317`，备份 `config-svc.backup-20260923.yml` 已存在；用户允许停用该旧子域，但配置尚未更改。`read.taostudioai.com` 当时错误指向 A `127.0.0.1`，公网不可达。只有 Tunnel 运行凭据，无可用的 Cloudflare DNS/API 管理凭据；用户已表示可登录账户，**下一位需经账户会话或用户操作核对 zone/记录/现有 Tunnel，不能凭本机 tunnel 凭据声称已改 DNS**。根域和邮件/主站记录不应受影响。

- **G1 拓扑**：常开机 SQLite 生产独立库 + 持久媒体卷，Fastify `127.0.0.1:8787`，本机反代（清单给出 Caddy 示例）`127.0.0.1:8080` 同源分发 `/api/*` 与 SPA，Cloudflare Tunnel 公共 hostname `read.taostudioai.com`。生产配置 `TAO_ALLOWED_ORIGIN=https://read.taostudioai.com`，可信代理按实际链路测试；周报调度单进程部署，避免多实例重复执行。静态前端能访问并不代表 API、SQLite 或私有媒体可用。
- **G2 持久化与恢复**：先定空库还是迁移旧家庭，查明仓库 `dev.db` 来源（此前一次本轮误触迁移）及 Prisma `file:` 实际绝对路径。用副本完成迁移、内容初始化、数据库一致快照+媒体同步备份、生产密钥保管/恢复、家庭删除、断电重启、磁盘不足/失联重连演练；测恢复后的家长/孩子、私有书/音频/进度、RPO/RTO。开机无人登录时 Node/Caddy/cloudflared 启动及失败自动恢复，监控磁盘、CPU/内存、上行带宽、HTTP 错误、数据库与媒体增长；敏感票据/Token 不写日志。备份要有离机/不同故障域副本；同机复制不是完整灾备。零新增预算并非零风险/零电费/零域名续费。
- **G3 账户/DNS**：先截图/导出 zone 现有 A/AAAA/CNAME/MX/TXT 与 Tunnel 配置；核对 `cpa` 真实业务归属及旧服务是否仍有人用。先在受限测试 hostname/Access 实测本机反代与站点，再在对应 Tunnel 加 `read → http://127.0.0.1:8080`，将错误的 `read` A 记录替换为 Cloudflare 控制台创建的 Tunnel 对应 DNS；只改此记录，按已有授权在安全切换窗口停用 `cpa`。若当前账户无 DNS 权限，记录具体操作和需用户在控制台完成的记录，不发明 API token。公网验证：DNS 正确、HTTPS 证书、首页/刷新深链、`/api/health`、登录/两家庭隔离、导入和续读、媒体超 5 分钟票据更新/Range、手机蜂窝网、关机/重启恢复、错误页及日志脱敏。保留旧配置与生产快照；回退时先冻结写入并保存上线后数据，再撤销 `read` hostname/DNS，不盲目覆盖新数据。
- **G4 免费产品边界**：Cloudflare R2 Standard 当时免费档 10 GB-month、月 100 万 Class A/1000 万 Class B；D1 免费档 5 GB、每日 500 万行读/10 万行写；Workers Free 每日 10 万请求，**接手者应以当期官方页面和账户控制台重新确认额度/超限及媒体用途条款**。现有 Node + Prisma SQLite 不能把 D1 当 SQLite 连接串直接替换；R2 可作为以后私有媒体存储评估，但需要对象权限、URL、生命周期、迁移校验、回退与独立备份。Supabase 免费档可能适合新的托管架构，却不自动提供本产品的全栈 Node 常开服务、现有媒体与无损数据库迁移。首发无需购买数据库，可先用自有机器，但上线须满足备份、可恢复与权限门槛。
- **G5 发布治理**：干净安装、迁移、`npm run verify`、前端 build、真实 E2E、隔离演练、CI 触发/分支保护、生产依赖公告、性能容量与安全扫描；保留 release SHA + 工作区变更来源和回滚包。旧部署文档 `docs/29-线上部署方案-read.taostudioai.com.md` 是付费 Linux VPS 路线，不要与当前零新增预算混用。当前未部署成功，不得在交付报告打勾。

## 9. F01–F43 原审计闭环矩阵（2026-09-23 本地核对）

| ID | 当前状态与准确剩余 | 执行包/发布门槛 |
|---|---|---|
| F01 | 部分：独立 parentCode 已有；旧家庭安全找回、防枚举、迁移/回滚欠缺。 | A1/P0 |
| F02 | 部分：进度 GET、收藏写入已检查可读权限；旧缓存、所有衍生媒体/屏蔽瞬时失效及全路由直达待测。 | A2/P0 |
| F03 | 部分：家长生成和 family scene 检查已存在；公开素材审核/发布/撤回、历史归属与费用保护欠缺。 | A2/A3/P0 |
| F04 | 部分：sid 与注销/撤销服务端检查已做；旧 token 恢复、设备 UI、多实例失效时效欠缺。 | A1/P0 |
| F05 | 部分：私人 TTS 归属/票据已有；并发幂等、预算/取消/存储上限及旧缓存待验。 | A3/P0 |
| F06 | 部分：trustProxy 可配；可信代理链及真实 IP/共享出口限流欠缺。 | A4/G3/P0 |
| F07 | 待复核：当期 npm 公告/升级/兼容证据欠缺；旧数字不是本日扫描。 | A4/G5/P0 |
| F08 | 部分：realpath/白名单/Range 已有；条件请求、资源上限、私有票据/播放器实测欠缺。 | A2/A4/P1 |
| F09 | 未闭环：详情/阅读深链独立加载及 403/404/加载/失败态待实测或修。 | B2/P1 |
| F10 | 未闭环：首页/详情续读目标和最近时序不统一。 | B2/P0 |
| F11 | 实现修复：心情枚举统一，仍须真实结束流程和失败重试浏览器回归。 | E1/P1 |
| F12 | 部分：显式末章完成、回看不清标记已做；轮次/完成事件/报告/迁移/并发欠缺。 | B1/P0 |
| F13 | 未闭环：主阅读器失败提示、可靠离开提交、双设备 revision 缺；家庭阅读器亦有手动保存缺口。 | B2/B3/P0 |
| F14 | 未闭环：报告/记忆刷新与家庭时区/午夜语义。 | B1/E4/P1 |
| F15 | 未闭环：跨截止动态休息规则、后台时长与统计标签。 | B1/P1 |
| F16 | 部分：整章英文 lang 已有；voice/provider/model/文本版本缓存和真人听审待补。 | E3/P1 |
| F17 | 未闭环：AudioSession/Abort/stream done/乱序取消链和 note 朗读块。 | E3/P1 |
| F18 | 未闭环：纸书占位与旧功能入口去留、真路径。 | E4/P1 |
| F19 | 未闭环：微信读书授权说明、失败重试、解绑及缓存清理。 | E4/P1 |
| F20 | 未闭环：查询状态与筛选、异步响应竞态、URL/空态。 | B2/P1 |
| F21 | 未闭环：年龄、英语解码难度、自主阅读能力混用与空书架主动作。 | D1/E4/P1 |
| F22 | 未闭环：词卡上下文、发音、移除、语言唯一键/迁移。 | E4/P1 |
| F23 | 未闭环：详情重复 Shell，深链/移动布局仍需修。 | E1/P1 |
| F24 | 未闭环：手机隐藏试听/喜欢等次级核心操作。 | E1/P1 |
| F25 | 未闭环：阅读双滚动/底栏横溢，真实尺寸复测。 | E1/P1 |
| F26 | 部分：静态触达现为 0 违规；真实触点、字体/层级、200% 缩放未验。 | E1/P1 |
| F27 | 未闭环：弹层语义、焦点限制/恢复、Esc/inert、读屏。 | E2/P1 |
| F28 | 未闭环：家长图源、周报状态/来源及中文语义。 | E4/P2 |
| F29 | 部分：CSS 静态检查已过；设计规范/token/KitchenSink/computed style 不一致。 | E1/P2 |
| F30 | 未闭环：逐书权利 manifest、署名链、地域/译者/改写、签认。 | F1/P0 |
| F31 | 未闭环：文本版本全本/节选/改写及质量、儿童适读人工审校。 | F1/P0 |
| F32 | 未闭环：seed 原子性、版本/稳定键、旧游标迁移与回滚。 | F2/P0 |
| F33 | 已实现待独立复核：sharp 声明/锁定、旧记录干净安装通过；接手重放。 | G5/P1 |
| F34 | 部分：初始化命令及文档已有；生产新/旧库真实回滚、内容媒体同步欠缺。 | F2/G2/P0 |
| F35 | 部分：E2E 隔离开关已有；现有服务、默认端口及付费配置继承风险需实测。 | G5/P0 |
| F36 | 部分：本地单测/lint/typecheck/build/touch 通过；完整 E2E、远端 CI 与分支保护未验。 | G5/P0 |
| F37 | 未闭环：大组件与父子端依赖倒置逐步拆分，须保留行为回归。 | E1/P2 |
| F38 | 未闭环：一致备份/恢复、密钥恢复、RPO/RTO、删除生命周期。 | G2/P0 |
| F39 | 未闭环：阅读/音频离线、PWA 安全更新与数据清理；现仅静态离线范围待明确。 | G5/P2 |
| F40 | 未闭环：设备/网络/负载、资源/费用/错误率/SLO 基线。 | G2/G5/P1 |
| F41 | 未闭环：儿童数据目的、保存期、第三方传输、导出删除/家长告知。 | G2/F3/P0 |
| F42 | 部分：新部署资料已有；README、代码注释、入口承诺与当前路由不一致。 | F3/G5/P1 |
| F43 | 未闭环：亲子家庭任务试点与归因、迭代记录。 | E4/G5/P1 |

## 10. T01–T13 交付映射与新增任务

| 原包 | 当前剩余及新增对应 | 可关闭的证据 |
|---|---|---|
| T01 | WP-G5、A4：干净安装、当期依赖审计、CI/E2E；静态 touch 已转 0。 | 独立环境启动与持续集成记录。 |
| T02 | WP-A1：旧家长恢复、会话撤销与 UI、防枚举。 | 旧库副本迁移 + 设备/跨家庭用例。 |
| T03 | WP-A2/A3/A4：统一权限、历史媒体、公开发布、预算。 | 角色矩阵、文件票据/费用并发测试。 |
| T04 | WP-B1/B2：完成历史、进度/事务/多端口径。 | 幂等/乱序/迁移/报告实测。 |
| T05 | WP-B2/B3：深链、续读、查询与家庭书保存。 | 手机、浏览器、断网/刷新可续读。 |
| T06 | WP-E3：音频状态机/缓存版本/真人听审。 | 慢网/切换/取消/语言听审。 |
| T07 | WP-E1/E2：Shell/手机/弹层/可访问性；静态 touch 已过。 | 多视口、键盘/读屏/缩放证据。 |
| T08 | WP-F1/F2：逐书权利与文本审签、原子版本发布。 | manifest 人工签名和故障注入。 |
| T09 | WP-E4：纸书/WeRead/词卡/家长报告闭环。 | 从主入口完成家庭任务。 |
| T10 | WP-G1/G2/G3/G4/G5：零预算实机上线和恢复、指标、隐私、PWA 范围。 | 生产恢复与公网验收/回退记录。 |
| T11 | WP-D1/D2：已有 2 课/2 篇**未审草稿**，需 8 课、音频/读物版本与教师审签。 | GPC 校验/音频逐条审查/权利台账。 |
| T12 | WP-D3：已有开关/API/试题页**草稿**，需教学路径、恢复、错题与报告。 | 一课全流程→8 课家庭试点。 |
| T13 | 全包发布闭环：逐 Fxx 标 fixed/accepted/deferred/not reproduced，所有延期在产品入口/承诺中明确。 | 合并后自动+真机+恢复+内容签认+发布决策。 |
| 新增 C1–C3 | TXT/EPUB/PDF/来源抓取与权利：原 T08/T09 未覆盖格式与家庭私有游标。 | 真实文件和源端到端/撤回演练。 |
| 新增 G3 | `read` DNS/Tunnel 与 `cpa` 安全退役。 | 目标域名公网实际可访问、主站未受影响。 |

## 11. 发给接手 agent 的开工指令与交付模板

```text
目标：在 D:\codesolo\taoread 按《未完成工作全量交接与执行验收》落实指定 WP-__，保留当前所有已修改/未跟踪文件与正在运行的服务。先读当前 AGENTS.md、git status/HEAD、本文件及所涉 Fxx 原审计，重新复现和确认最新状态。以独立 DB/媒体目录/空闲端口及 mock 付费上游开发，先补失败用例后做最小修复。不得把旧复核表当现状。跨迁移或共享组件协调唯一集成负责人。落实格式/版权/课程/账户外部依赖时记录原始证据，缺专业签认则保持待审。仅在会话授权范围内操作 Git/部署；本项目用户此前明确提出部署 read 子域和停用 cpa，但实际账户、流量、迁移与回滚要先核实。完成后提供：变更文件清单；复现→修复证据；自动/浏览器/真机结果；迁移/回退；权限、费用和私密信息检查；未完成清单及发布门槛。不能将本地通过写为线上通过。
```

交付报告逐项使用 `ID/优先级/当前证据/修改/验收环境与结果/迁移或回退/专业或账号依赖/剩余风险/是否允许公开`。关键人工待办分别交给域名账户持有人、版权/内容审校者、英语拼读教学与语音审校者；Agent 可以制作可审核材料与工具，不能代签。本文若与实时源码或账户状态冲突，以复核结果为准并更新证据与状态，不能静默删项。
